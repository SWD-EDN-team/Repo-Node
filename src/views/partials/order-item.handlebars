<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="order-item d-flex align-items-center p-3 border rounded shadow-sm">
    <div class="d-flex flex-column me-5">
      <div class="d-flex align-items-center gap-3">
        <img src="/assets/images/dress.png" alt="{{name}}" class="img-fluid" style="width: 60px; height: 60px;">
        <div class="item-info">
          <h5 class="mb-1">{{name}}</h5>
          <p class="mb-1">Size: {{size}}</p>
          <p class="mb-1">Qty: {{qty}}</p>
        </div>
      </div>
      <div class="item-status {{statusClass}} mt-2">
        <button class="btn text-white me-2"
          style="background-color: {{#if (eq status 'Delivered') }}#3CD139{{else}}#E3B231{{/if}};">
          {{status}}
        </button>
        <span class="text-dark">Your product has been {{status}}</span>
      </div>
    </div>
    <div class="item-price me-4">
      <strong>${{price}}</strong>
    </div>
    <div class="order-actions d-flex gap-2">
      <button class="btn btn-outline-dark">View Order</button>
      {{#if showReview}}
        <button class="btn btn-primary">Write A Review</button>
      {{/if}}
      {{#if showCancel}}
        <button class="btn btn-danger">Cancel Order</button>
      {{/if}}
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>